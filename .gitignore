# macOS
.DS_Store
**/.DS_Store

# Environment
.env
**/.env
.env.local
**/.env*.local
!**/.env.local.example

# Build outputs
app/frontend/.next/
app/backend/build/
app/backend/.gradle/

# Gradle
**/.gradle/
**/build/

# Node
app/frontend/node_modules/
# 리포 루트에 생긴 것 — 에디터가 타입 확인용으로 만든 것이라 소스가 아니다 (26MB)
/node_modules/
/package.json
/package-lock.json
/next-env.d.ts

# 스크래치·산출물 (소스 아님)
.tmp/
*.pptx

# Logs
*.log

# Eclipse/IDE build output
app/backend/bin/

# Scratch files that landed in the tree by accident
app/SCR-*.png
app/iTerm2-Color-Schemes/

# Claude Code 워크트리 (격리 작업공간 — 소스 아님)
.claude/worktrees/
